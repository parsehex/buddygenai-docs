import{_ as t,c as n,o,ae as a}from"./chunks/framework.Cd-3tpCq.js";const p=JSON.parse('{"title":"Unifying buddyGenAI Versions","description":"","frontmatter":{},"headers":[],"relativePath":"dev/unifying-versions.md","filePath":"dev/unifying-versions.md"}'),i={name:"dev/unifying-versions.md"};function s(r,e,l,h,d,u){return o(),n("div",null,e[0]||(e[0]=[a('<h1 id="unifying-buddygenai-versions" tabindex="-1">Unifying buddyGenAI Versions <a class="header-anchor" href="#unifying-buddygenai-versions" aria-label="Permalink to &quot;Unifying buddyGenAI Versions&quot;">​</a></h1><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>One of the intentions behind buddyGenAI was to have an application that could introduce people to using AI which they have control and ownership of. I&#39;m often impressed by the kinds of models that I can run on my PC at home + I appreciate interfaces which are purpose-built but still manage to be versatile, so a pillar of my direction with this app has been to support using it in various ways. I&#39;d rather the app meet the user where they are rather than the user needing to meet the app.</p><p>For the first iteration, I used embedded software (llama.cpp and others) with an Electron wrapper, which means that you need to run the app and the AI on a single machine. Sometime after that, my setup changed and I wanted to be able to access the app from my laptop, so I threw together a single-page web app (accessible at <a href="https://app.buddygenai.com/" target="_blank" rel="noreferrer">https://app.buddygenai.com/</a>).</p><p>As of more recently, my desire is to be able to install buddyGenAI as a standalone server on my desktop and connect it to AI API(s) running on the same machine. Essentially, I want to use BGAI as my own self-hosted AI frontend.</p><h2 id="the-problem-s" tabindex="-1">The Problem(s) <a class="header-anchor" href="#the-problem-s" aria-label="Permalink to &quot;The Problem(s)&quot;">​</a></h2><p>There are a number of differences and issues between these to-be 3 &quot;form factors&quot; of the app.</p><ul><li>I think the use cases are different even. <ul><li><strong>Electron:</strong> Everything besides the models is self-contained. You&#39;ll use the app from (e.g.) your gaming PC and nowhere else.</li><li><strong>SPA:</strong> You don&#39;t have a gaming or otherwise powerful PC or don&#39;t want to use it, so you just use the hosted web app and hook it up to a service or a local API.</li><li><strong>Server (<em>new/to-do</em>):</strong> You have a powerful PC to run AI on, but you want to access the same instance from other machines (ideally on the same LAN).</li></ul></li><li>We use different DBs for the different versions &amp; access them differently. <ul><li>Electron version uses an SQLite DB, with the schema is stored in <code>/migrations/</code>, and SQL statements are actually constructed on the client/renderer and sent along with parameters to the main process to run. <ul><li>I did the SQL statements this way to keep as much logic as I could within <code>/src/</code>. Though I know that needs to be re-worked, it made converting to an SPA / using Dexie easier.</li></ul></li><li>The SPA uses Dexie, with the schema found at <code>/src/lib/db/schema.ts</code>.</li><li><a href="./database-info.html">More info here - Database Info</a></li></ul></li><li>Calling the AI / Running inference isn&#39;t the same. <ul><li><strong>SPA</strong> has to make HTTP calls to an API (ignoring projects using WASM/WebGPU).</li><li>Both <strong>Electron</strong> and <strong>Server</strong> versions could use APIs <strong>or</strong> local inference by calling binaries directly.</li><li>Separate but related note: This would be a QoL thing, but it would be nice to allow pointing the (Electron and Server) app to your own binaries, so that you can supply your own e.g. <code>llama-server</code>.</li><li>There are UI differences depending on how much we an control the AI that we&#39;re using. <ul><li>KoboldCpp now has hot-swappable models via switching configs, so we can add support for that. Still, I think we&#39;d want alternate components for different AI providers.</li></ul></li></ul></li><li>There&#39;s also much messiness around the <code>AppSettings.ts</code> file(s). It&#39;s meant to be an interface to access the DB-persisted settings that you can access via the UI. <ul><li>At some point I noticed buggy behavior surrounding boolean and number values being cast unexpectedly when pulling from the DB. I wasn&#39;t able to pinpoint the issue and now there are several places with extra checks and conditional operations to guard against this. SQLite only supports storing booleans as 0 or 1, which I don&#39;t think helps the situation.</li><li>As mentioned in <a href="./database-info.html">Database Info</a>, I think I want to use Dexie for all the app&#39;s versions, which should fix these issues.</li></ul></li><li>Finally, a point that I would explicitly mention is that overall, the app isn&#39;t well-designed to handle conditional features (e.g. if you don&#39;t hook up image generation, it still presents the option to generate a profile image when creating a Buddy). This would apply to using KoboldCpp with the SPA but also the Electron version since the user still needs to provide a model.</li></ul><h2 id="future" tabindex="-1">Future <a class="header-anchor" href="#future" aria-label="Permalink to &quot;Future&quot;">​</a></h2><p>Eventually I want to explore having the electron version be its own repo, just for the electron-specific parts.</p><p>I&#39;m recently thinking of having a separate repo for the electron / tauri version, providing just the wrapper while using the main repo to pull in the app&#39;s code.</p>',11)]))}const g=t(i,[["render",s]]);export{p as __pageData,g as default};
